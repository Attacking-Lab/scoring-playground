services:
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    restart: unless-stopped
    volumes:
      - ./upstream:/upstream
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
